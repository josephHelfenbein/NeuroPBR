# Mobile App
Flutter-based iOS app for scanning real-world surfaces and generating PBR maps.

- Integrates Core ML model for on-device inference.
- Uses Objective-C++ hooks to call the Metal-based renderer.
- Provides a simple interface for capture, preview, and export of materials.
