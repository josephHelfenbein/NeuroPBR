# CoreML

Holds Core ML model conversion, quantization, and deployment scripts.

- Converts trained PyTorch student models to `.mlmodel` format.
- Optimizes models for iOS inference (e.g., float16 quantization).
- Integrated into CI/CD for automatic Core ML builds and version tracking.
