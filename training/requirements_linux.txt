# Core dependencies for Linux
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0
einops==0.8.1
pytest==8.4.2
timm==1.0.21
numpy>=1.24.0
tqdm>=4.65.0
tensorboard>=2.12.0
Pillow>=10.0.0

# Linux specific
triton==3.5.0

# Optional dependencies
# Uncomment if you want to use these features:

# For WandB logging (set use_wandb=True in config)
# wandb>=0.15.0

# For advanced visualization (used by visualization.py if available)
# opencv-python>=4.8.0
# matplotlib>=3.7.0

# For distributed training (when implemented)
# accelerate>=0.20.0
